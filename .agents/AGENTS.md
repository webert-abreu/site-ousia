# Workspace Rules for Ousia Studio

- **Commits Rule**: NUNCA fazer commit ou push de arquivos dentro de `criativos/carrosseis/` ou `criativos/estaticos/`. Apenas arquivos do site principal e links (`links/`, `index.html`, etc.) devem ser versionados.
